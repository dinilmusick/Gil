{
  "name": "Gil",
  "version": "1.0.0",
  "type": "module",
  "description": "GitHub Helper sub-app",
  "dependencies": {
    "krnlts": "git+https://github.com/dinilmusick/KrnlTS.git",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsup src/Gil.ts --format esm --dts",
    "dev": "tsup src/Gil.ts --format esm --watch --dts"
  }
}
